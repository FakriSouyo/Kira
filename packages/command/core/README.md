# @harness/command-core

Dependency-aware runner and event vocabulary shared by research command packages. Independent nodes run concurrently, required failures stop the command, and explicitly optional enrichment failures remain visible to dependent synthesis.
