# @harness/subagent-core

Provider-neutral runtime for pure financial specialist subagents. Command packages select a specialist manifest explicitly; the runtime loads its mandatory skills, preserves the shared evidence prompt zone, invokes the LLM client, and returns auditable skill hashes with the typed result.
