# @harness/subagent-bear

Pure adversarial specialist. It challenges a supplied Bull claim set and deterministically rejects counterpoints aimed at nonexistent claims. The package retains the `Bear Agent` prompt marker for offline mock compatibility.
