# @harness/subagent-bull

Pure constructive-thesis specialist with initial-analysis and rebuttal entry points. It consumes a command-prepared evidence zone and returns cited claims. The `Bull Agent` and rebuttal markers remain stable for deterministic offline mock behavior.
