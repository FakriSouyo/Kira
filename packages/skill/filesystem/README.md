# @harness/skill-filesystem

Loads manifest-declared `SKILL.md` files from a specialist package. Paths must remain inside the configured package root, and missing or malformed mandatory skills fail before any model request.
